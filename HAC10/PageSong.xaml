<Page
    x:Class="HAC10.PageSong"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HAC10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <RadioButton Content="Mới cập nhật" Style="{StaticResource NavRadioButtonStyle}" Tag="&#xE72C;" IsChecked="True"></RadioButton>
            <RadioButton Content="Phổ biến" Style="{StaticResource NavRadioButtonStyle}" Tag="&#xE728;"></RadioButton>
        </StackPanel>

        <ListView Grid.Row="1" Name="ListViewRecentSongs" 
                  ItemTemplate="{StaticResource ItemTemplate}">
        </ListView>
    </Grid>
</Page>
